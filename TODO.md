# Roadmap

- Add an explicit venue-adapter schema without network clients.
- Add tick-size decimal validation as an opt-in input contract.
- Add multi-level OFI variants with literature-linked definitions and separate names.
